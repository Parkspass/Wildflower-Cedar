const state = {
  results: [],
};

const getters = {
  allResults: (state) => state.results,
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
